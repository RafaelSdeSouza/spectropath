% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classical-features.R
\name{classical_line_features}
\alias{classical_line_features}
\alias{line_flux}
\alias{peak_flux}
\alias{equivalent_width}
\alias{line_moments}
\alias{fwhm}
\alias{w80}
\alias{classical_features}
\title{Classical line-profile descriptors}
\usage{
line_flux(path)

peak_flux(path)

equivalent_width(path, continuum = 1)

line_moments(path, weight = c("positive", "absolute", "negative"))

fwhm(path, weight = c("positive", "absolute", "negative"))

w80(path, weight = c("positive", "absolute", "negative"))

classical_features(
  path,
  continuum = 1,
  weight = c("positive", "absolute", "negative")
)
}
\arguments{
\item{path}{Two-column numeric matrix or data frame coercible by
\code{\link[=as_spectral_path]{as_spectral_path()}}.}

\item{continuum}{Scalar or vector continuum level used by
\code{equivalent_width()}.}

\item{weight}{Weighting convention for moments and non-parametric widths:
\code{"positive"} uses positive flux only, \code{"absolute"} uses absolute flux,
and \code{"negative"} uses absorption depth \code{pmax(-F, 0)}.}
}
\value{
Numeric scalar, except \code{line_moments()} and \code{classical_features()},
which return data frames.
}
\description{
Convenience functions for benchmarking and machine-learning workflows.
These quantities are not path-signature features; they are provided so users
can compare classical summaries against the path-signature diagnostics in a
consistent table.
}
\details{
\code{equivalent_width()} returns a signed pseudo-equivalent width for a
continuum-subtracted or continuum-normalized profile. If \code{continuum = 1},
this is simply the integrated line flux in the coordinate units. For
conventional equivalent widths, provide the continuum level in the same units
as the flux.

Moment-like quantities use non-negative profile weights. For emission lines,
the default \code{weight = "positive"} uses \code{pmax(F, 0)}. For mixed
emission--absorption profiles, \code{weight = "absolute"} can be useful.
}

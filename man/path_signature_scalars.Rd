% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path-features.R
\name{path_signature_scalars}
\alias{path_signature_scalars}
\alias{levy_area}
\alias{curl_u}
\alias{curl_f}
\alias{skew_signature}
\alias{jerk_u}
\alias{twist_uf}
\alias{jerk_f}
\alias{emabs_area}
\title{Path-signature diagnostics for spectral-line morphology}
\usage{
levy_area(path, normalize = TRUE)

curl_u(path, normalize = TRUE)

curl_f(path, normalize = TRUE)

skew_signature(path, normalize = TRUE)

jerk_u(path, normalize = TRUE)

twist_uf(path, normalize = TRUE)

jerk_f(path, normalize = TRUE)

emabs_area(
  path,
  normalize = TRUE,
  orient = c("increasing", "as_is"),
  eps = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{path}{Two-column numeric matrix or data frame coercible by
\code{\link[=as_spectral_path]{as_spectral_path()}}.}

\item{normalize}{Logical; if \code{TRUE}, divide each contrast by its natural
coordinate and flux ranges.}

\item{orient}{If \code{"increasing"}, force the path to run from smaller to
larger coordinate before computing the cumulative emission--absorption
area. If \code{"as_is"}, preserve input order.}

\item{eps}{Numerical tolerance used to detect absent emission or absorption.}
}
\value{
A numeric scalar.
}
\description{
These functions expose the low-order path-signature quantities used in the
accompanying paper. They are computed from the log-signature of the
piecewise-linear path \verb{X = (u, F)}, where \code{u} is an ordered coordinate and
\code{F} is the measured signal or line flux.
}
\details{
The scalar functions use descriptive names, while \code{\link[=path_features]{path_features()}} returns
the compact paper notation by default:
\itemize{
\item \code{levy_area()} is \eqn{p_2}, the signed velocity--flux area.
\item \code{curl_u()} is \eqn{p_{3u}}, where signed area lies along the ordered
coordinate. In velocity-space applications this is \eqn{p_{3v}}.
\item \code{curl_f()} is \eqn{p_{3F}}, whether signed area lies in bright or faint
parts of the line.
\item \code{jerk_f()} is \eqn{p_{4F}}, higher-order flux modulation.
\item \code{twist_uf()} is \eqn{p_{4T}}, twist-like velocity--flux structure.
\item \code{emabs_area()} is \eqn{p_{\pm}}, emission--absorption ordering.
}

The additional helpers \code{skew_signature()} and \code{jerk_u()} are kept for
exploratory work and are available from \code{\link[=path_features]{path_features()}} with
\code{extended = TRUE}.
}
\examples{
u <- seq(-5, 5, length.out = 200)
f <- exp(-0.5 * (u / 0.8)^2) + 0.2 * exp(-0.5 * ((u - 2) / 1.2)^2)
path <- cbind(u, f)
levy_area(path)
curl_f(path)
}

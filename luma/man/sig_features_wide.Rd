% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sig_features_wide.R
\name{sig_features_wide}
\alias{sig_features_wide}
\title{Batch signature features for a "wide" matrix (rows = objects, cols = samples)}
\usage{
sig_features_wide(mat, axis = NULL, depth = 3, normalize = TRUE, id_col = "id")
}
\arguments{
\item{mat}{numeric matrix, rows = objects, cols = samples along a common axis}

\item{axis}{numeric vector for the horizontal axis (length = ncol(mat)).
If NULL, tries as.numeric(colnames(mat)); if that fails, uses seq_len(ncol(mat)).}

\item{depth}{integer in {2,3,4} for feature depth}

\item{normalize}{logical; pass through to scalar feature fns}

\item{id_col}{character; name of the id column in the output}
}
\value{
data.frame with one row per object and signature features as columns
}
\description{
Batch signature features for a "wide" matrix (rows = objects, cols = samples)
}

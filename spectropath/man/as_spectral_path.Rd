% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path.R
\name{as_spectral_path}
\alias{as_spectral_path}
\title{Coerce spectral data to an ordered two-dimensional path}
\usage{
as_spectral_path(
  x,
  u = NULL,
  flux = NULL,
  sort = TRUE,
  remove_nonfinite = TRUE
)
}
\arguments{
\item{x}{A two-column numeric matrix, or a data frame containing the columns
named by \code{u} and \code{flux}.}

\item{u, flux}{Column names to read when \code{x} is a data frame.}

\item{sort}{Logical; if \code{TRUE}, sort rows by the ordered coordinate.}

\item{remove_nonfinite}{Logical; if \code{TRUE}, remove rows containing \code{NA},
\code{NaN}, or infinite values.}
}
\value{
A two-column numeric matrix with columns \code{u} and \code{flux}.
}
\description{
\code{as_spectral_path()} converts a two-column matrix or data frame into the
path convention used by \code{spectropath}: an ordered coordinate \code{u} and a
measured signal or line flux \code{F}.
}
\details{
The coordinate \code{u} may be wavelength, rest-frame wavelength, log-wavelength,
phase, time, or velocity relative to a transition. For spectral-line
morphology, \code{F} is usually continuum-subtracted or continuum-normalized
flux, but the path-signature calculations only require an ordered
two-dimensional curve.
}
\examples{
d <- data.frame(lambda = c(5002, 5001, 5003), flux = c(0.1, 0.0, 0.2))
as_spectral_path(d, u = "lambda", flux = "flux")
}
